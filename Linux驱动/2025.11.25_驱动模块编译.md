- 一般的驱动代码包含以下几部分
	1. 头文件
```
	#include<linux/init.h> 宏定义
	#include<linux/module.h> 初始化加载模块
```
	2. 驱动模块入口、出口（声明）
```
	module_init(hello_init);
    module_exit(hello_exit);
```
	3. 声明模块拥有开源许可证
```
    module_license("GPL");
```
	4. 模块内容与实现

- 驱动编译的方法有两个
1. 驱动编译为模块，通过命令把驱动加载到内核
2. 直接把驱动编译到内核